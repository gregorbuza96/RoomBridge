apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: roombridge
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8081"
    spec:
      containers:
        - name: user-service
          image: YOUR_ECR_ACCOUNT.dkr.ecr.YOUR_REGION.amazonaws.com/user-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: CONFIG_SERVER_URL
              value: http://config-server:8888
            - name: DB_HOST
              value: db-users
            - name: DB_USER
              value: roombridge
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: roombridge-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: roombridge-secrets
                  key: jwt-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: roombridge
spec:
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
